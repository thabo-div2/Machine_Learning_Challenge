import numpy as np

# A program that computes mean, standard deviation and variance
num = np.arange(0, 21)
mean = np.mean(num)
std_dev = np.std(num)
variance = np.var(num)

print(num)
print(mean)
print(std_dev)
print(variance)
